# Cloudflare Pages _redirects
  # Proxy /api/* to the Loop API Cloudflare Worker
  # Without this, /api/* requests hit Pages (which returns the SPA HTML instead of Worker JSON)
  /api/*  https://loop-api.rald.cloud/api/:splat  200
  